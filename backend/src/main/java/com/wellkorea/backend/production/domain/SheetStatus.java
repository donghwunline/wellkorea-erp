package com.wellkorea.backend.production.domain;

/**
 * Status of a work progress sheet (aggregate status of all steps).
 */
public enum SheetStatus {
    NOT_STARTED,
    IN_PROGRESS,
    COMPLETED
}

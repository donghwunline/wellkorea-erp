# CD - Development Environment Deployment
#
# This workflow is commented out and ready to be enabled when you're ready to deploy to dev.
# To enable: uncomment the entire workflow below and configure your deployment secrets.
#
# Required secrets:
# - DEV_SSH_HOST: SSH host for dev server
# - DEV_SSH_USER: SSH user for dev server
# - DEV_SSH_KEY: SSH private key for dev server
# - SONAR_TOKEN: SonarCloud token (already configured)

# name: CD - Dev Environment
#
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:
#
# # Prevent concurrent deployments
# concurrency:
#   group: dev-deployment
#   cancel-in-progress: false
#
# jobs:
#   # Build and push Docker images
#   build-backend:
#     name: Build Backend
#     uses: ./.github/workflows/_shared/docker-build.yml
#     with:
#       component: backend
#       environment: dev
#       push: true
#     permissions:
#       contents: read
#       packages: write
#
#   build-frontend:
#     name: Build Frontend
#     uses: ./.github/workflows/_shared/docker-build.yml
#     with:
#       component: frontend
#       environment: dev
#       push: true
#     permissions:
#       contents: read
#       packages: write
#
#   # Deploy to dev environment
#   deploy-dev:
#     name: Deploy to Dev
#     runs-on: ubuntu-latest
#     needs: [build-backend, build-frontend]
#     environment:
#       name: dev
#       url: https://dev.wellkorea-erp.com  # Update with your dev URL
#
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#
#       - name: Create deployment directory
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.DEV_SSH_HOST }}
#           username: ${{ secrets.DEV_SSH_USER }}
#           key: ${{ secrets.DEV_SSH_KEY }}
#           script: |
#             mkdir -p ~/wellkorea-erp
#             cd ~/wellkorea-erp
#
#       - name: Copy docker-compose.yml to server
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.DEV_SSH_HOST }}
#           username: ${{ secrets.DEV_SSH_USER }}
#           key: ${{ secrets.DEV_SSH_KEY }}
#           source: "docker-compose.yml,.env.example"
#           target: "~/wellkorea-erp"
#
#       - name: Deploy with Docker Compose
#         uses: appleboy/ssh-action@v1.0.3
#         env:
#           GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         with:
#           host: ${{ secrets.DEV_SSH_HOST }}
#           username: ${{ secrets.DEV_SSH_USER }}
#           key: ${{ secrets.DEV_SSH_KEY }}
#           envs: GHCR_TOKEN
#           script: |
#             cd ~/wellkorea-erp
#
#             # Login to GitHub Container Registry
#             echo "$GHCR_TOKEN" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
#
#             # Pull latest images
#             docker compose pull
#
#             # Deploy
#             docker compose up -d
#
#             # Clean up old images
#             docker image prune -af
#
#       - name: Wait for services to be healthy
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.DEV_SSH_HOST }}
#           username: ${{ secrets.DEV_SSH_USER }}
#           key: ${{ secrets.DEV_SSH_KEY }}
#           script: |
#             cd ~/wellkorea-erp
#             timeout 120 bash -c 'until docker compose ps | grep -q healthy; do sleep 5; done'
#
#   # Run E2E tests against dev environment
#   e2e-tests:
#     name: E2E Tests
#     needs: deploy-dev
#     uses: ./.github/workflows/_shared/e2e-tests.yml
#     with:
#       environment: dev
#       base-url: https://dev.wellkorea-erp.com  # Update with your dev URL
#
#   # Deployment summary
#   deployment-summary:
#     name: Deployment Summary
#     runs-on: ubuntu-latest
#     needs: [deploy-dev, e2e-tests]
#     if: always()
#
#     steps:
#       - name: Create deployment summary
#         run: |
#           echo "## Dev Deployment Summary" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "**Environment:** Development" >> $GITHUB_STEP_SUMMARY
#           echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
#           echo "**Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#
#           if [ "${{ needs.deploy-dev.result }}" == "success" ]; then
#             echo "✅ Deployment: Success" >> $GITHUB_STEP_SUMMARY
#           else
#             echo "❌ Deployment: Failed" >> $GITHUB_STEP_SUMMARY
#           fi
#
#           if [ "${{ needs.e2e-tests.result }}" == "success" ]; then
#             echo "✅ E2E Tests: Passed" >> $GITHUB_STEP_SUMMARY
#           else
#             echo "❌ E2E Tests: Failed" >> $GITHUB_STEP_SUMMARY
#           fi

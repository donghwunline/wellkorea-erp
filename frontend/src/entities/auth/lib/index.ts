/**
 * Auth Lib - Public API.
 *
 * Re-exports auth-related utilities from shared.
 * Provides a stable import path for auth entity consumers.
 */

// Re-export from shared/utils for convenience
export { authStorage } from '@/shared/utils';

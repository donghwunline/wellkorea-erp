/**
 * Entity type enum and display configuration.
 *
 * Re-exports from @/shared/domain for backwards compatibility.
 * New code should import directly from @/shared/domain.
 */

export {
  EntityType,
  EntityTypeConfigs,
  getEntityTypeLabel,
  type EntityTypeConfig,
} from '@/shared/domain';

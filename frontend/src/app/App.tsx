/**
 * Main Application Component.
 *
 * This is the root component that renders the application router.
 * All providers are handled by AppProviders in main.tsx.
 */

import { AppRouter } from './router';

function App() {
  return <AppRouter />;
}

export default App;
